{
	"name": "astrojobs",
	"version": "0.0.0",
	"private": true,
	"workspaces": [
		"./apps/*"
	],
	"scripts": {
		"build": "dotenv -- turbo run build",
		"dev": "dotenv -- turbo run dev",
		"lint": "turbo run lint",
		"format": "prettier --write \"**/*.{ts,tsx,md}\""
	},
	"dependencies": {
		"turbo": "^1.8.8"
	},
	"devDependencies": {
		"dotenv-cli": "^7.2.1"
	}
}
